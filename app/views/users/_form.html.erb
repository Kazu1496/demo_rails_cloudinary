<%= form_for(@user) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <!-- CarrierWaveのみでの画像アップロード用 -->
  <!-- <div class="field">
    <%= f.label :image %>
    <%= f.file_field :image %>
  </div> -->

  <!-- CarrierWave + Cloudinaryでの画像アップロード用 -->
  <div class="field">
    <%= f.label :cl_image %>
    <%= f.file_field :cl_image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
